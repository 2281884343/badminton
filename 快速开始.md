# ⚡ 快速开始

## 🎯 最简单的部署方法

### 1. 准备工作

确保你的Linux服务器已安装：
- Python 3.8+
- Node.js 16+

如果没有安装：
```bash
# Ubuntu/Debian
sudo apt update
sudo apt install python3 python3-pip python3-venv nodejs npm -y

# CentOS/RHEL
sudo yum install python3 python3-pip nodejs npm -y
```

### 2. 下载代码

```bash
# 如果有git仓库
git clone <your-repo-url> badminton-game
cd badminton-game

# 或者上传代码文件夹到服务器
```

### 3. 一键部署

```bash
# 赋予执行权限
chmod +x deploy.sh start-dev.sh start-prod.sh

# 运行部署脚本
./deploy.sh
```

这个脚本会自动：
✓ 检查环境
✓ 创建虚拟环境
✓ 安装所有依赖
✓ 创建必要的目录

### 4. 启动服务

#### 开发模式（测试用）
```bash
./start-dev.sh
```
访问：http://your-server-ip:3000

#### 生产模式（推荐）
```bash
./start-prod.sh
```

## 📝 三步走生产部署

如果要部署到生产环境，推荐使用Nginx反向代理：

### 第1步：构建前端
```bash
cd frontend
npm run build
```

### 第2步：配置Nginx
```bash
# 修改nginx.conf中的路径
vi nginx.conf

# 复制配置到Nginx
sudo cp nginx.conf /etc/nginx/sites-available/badminton-game
sudo ln -s /etc/nginx/sites-available/badminton-game /etc/nginx/sites-enabled/

# 测试并重启
sudo nginx -t
sudo systemctl restart nginx
```

### 第3步：启动后端服务
```bash
# 使用systemd管理服务
sudo cp badminton-game.service /etc/systemd/system/
# 修改服务文件中的路径
sudo vi /etc/systemd/system/badminton-game.service
# 启动服务
sudo systemctl enable badminton-game
sudo systemctl start badminton-game
```

完成！访问：http://your-domain.com

## 🐳 Docker一键部署（最简单）

如果你的服务器有Docker：

```bash
# 安装Docker和Docker Compose
curl -fsSL https://get.docker.com | sh
sudo systemctl start docker
sudo usermod -aG docker $USER

# 构建并启动
docker-compose up -d

# 查看状态
docker-compose ps
```

访问：http://your-server-ip

详见：[Docker部署.md](./Docker部署.md)

## 🎮 开始游戏

1. 打开浏览器访问你的服务器地址
2. 输入用户名并设置技能熟练度
3. 创建房间或加入房间
4. 开始游戏！

## 🔧 常见问题

### 端口被占用
```bash
# 查看占用端口的进程
sudo lsof -i :8080
sudo lsof -i :3000

# 结束进程
sudo kill -9 <PID>
```

### 防火墙阻止
```bash
# 开放端口
sudo ufw allow 80/tcp
sudo ufw allow 8080/tcp
sudo ufw allow 3000/tcp
```

### WebSocket连接失败
确保Nginx配置了WebSocket支持（已在nginx.conf中配置）

## 📚 更多信息

- 详细部署教程：[部署指南-Linux.md](./部署指南-Linux.md)
- Docker部署：[Docker部署.md](./Docker部署.md)
- 项目说明：[项目说明.md](./项目说明.md)
- 完整文档：[README.md](./README.md)

## 💡 提示

- 首次部署需要几分钟安装依赖
- 开发模式用于测试，生产模式用于正式环境
- 记得修改API密钥（backend/main.py）
- 建议使用Nginx和systemd部署生产环境

---

**5分钟即可上线！🚀**

